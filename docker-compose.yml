version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: khanyiso-portfolio
    restart: unless-stopped
    environment:
      - NODE_ENV=production